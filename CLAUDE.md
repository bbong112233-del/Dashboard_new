# 작업 규칙 (Dashboard_new)

## 커밋 전 보안 검토 및 승인 — 필수

코드를 수정한 뒤 **커밋하기 전에 반드시** 아래 순서를 지킨다.

1. 보안 검토를 수행하고 결과를 요약해 보고한다.
2. 사용자의 **명시적 승인**을 받는다.
3. 승인 후 커밋한다.
4. **푸시는 커밋과 별개로 다시 승인**을 받는다.

승인 없이 커밋하거나, "이 정도는 괜찮겠지"라는 판단으로 건너뛰지 않는다.

### 보안 검토 체크리스트

변경된 diff를 기준으로 아래 항목을 확인한다.

- **자격증명 노출**: 새로 하드코딩된 API 키, 토큰, 비밀번호, 서비스 계정 키가 있는가
- **개인정보·영업정보**: 실제 거래처명, 담당자명, 이메일, 전화번호, 매출 실수치가 코드나 주석에 박혀 있는가 (샘플·테스트 데이터 포함)
- **Firebase 접근 범위**: `FIREBASE_CONFIG`, `database.rules.json`, 팀 경로(`teams/...`) 관련 변경이 다른 팀 데이터에 접근을 열어주지 않는가
- **데이터 삭제 로직**: 자동 삭제·정리 코드가 의도한 범위(기간·대상)만 지우는가, 팀 공유 데이터에 영향을 주는가
- **외부 리소스**: 새로 추가된 CDN·스크립트·이미지 출처가 신뢰할 수 있는가
- **XSS**: 사용자 입력(메모, 공지, 거래처명)을 `innerHTML`에 넣을 때 이스케이프(`esc`, `v3_esc`)를 거치는가
- **로그 노출**: `console.log`로 민감 데이터가 찍히지 않는가
- **커밋 대상**: 스테이징된 파일에 임시 파일, 백업, `.DS_Store`, 로컬 설정이 섞여 있지 않은가

### 보고 형식

```
보안 검토 결과
- 자격증명: 신규 노출 없음 / [발견 시 파일·라인]
- 개인정보: ...
- (해당되는 항목만 나열, 위험 있으면 먼저 알림)
판단: 커밋 가능 / 수정 필요
```

## 저장소 구조 주의

- 이 프로젝트는 **같은 원격(`bbong112233-del/Dashboard_new`)을 쓰는 클론이 2개** 있다.
  - `~/Documents/GitHub/WEB 작업` — GitHub Desktop이 추적하는 작업본
  - `~/Documents/GitHub/WEB Project` — 별도 클론
- 수정 전에 어느 폴더를 대상으로 하는지 먼저 확인한다. GitHub Desktop은 폴더명이 아니라 원격 저장소명(`Dashboard_new`)으로 표시되므로 혼동하기 쉽다.

## 검증

- 배포물은 단일 HTML 파일(`Firebase_실시간버전/매출대시보드_Firebase.html`)이다.
- 스크립트를 수정하면 커밋 전에 문법 검사를 거친다(`<script>` 블록 추출 후 `node --check`).
- 정렬·필터처럼 화면 결과로만 확인되는 로직은 상태값만 보고 판단하지 말고 실제 정렬 결과를 확인한다.
